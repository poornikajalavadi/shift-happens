# Intermediate trained model files — excluded due to file size constraints
models/best_model_*.pkl

# Final production-ready debiased model — included for repository reproducibility
!models/final_model_debiased.pkl

# Raw and processed datasets — managed via DVC, not tracked in Git
data/

# Runtime execution logs — generated on each pipeline run
logs/

# Python bytecode and cache directories
__pycache__/
*.pyc
*.pyo

# Virtual environment directory
.venv/

# MLflow experiment tracking directory — generated at runtime
mlruns/
